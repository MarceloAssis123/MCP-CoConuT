# Configurações do CoConuT

# Ativa a persistência de dados (padrão: false)
# Valores aceitos: true, 1, yes, y, on (case insensitive)
PERSISTENCE_ENABLED=false

# Caminho para o diretório de armazenamento de dados
# Se não definido, será usado './coconut-data' como padrão quando a persistência estiver ativada
STORAGE_FILE_PATH=./coconut-data

# Outras configurações podem ser adicionadas no futuro
# Por enquanto, as demais configurações são feitas diretamente no arquivo config.ts 